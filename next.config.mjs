/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // turbopack: {
  //   root: process.cwd(),
  // },
    // basePath: "/mandalart",
    // assetPrefix: "/mandalart/",
    // output: "export",
    
};

export default nextConfig;
